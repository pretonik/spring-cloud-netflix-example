spring:
  application:
    name: configserver
  config:
    name: configserver 
  cloud:
    config:
      enabled: true
encrypt:
   key: foobar
#  failOnError: false
#  keyStore:
#    location: classpath:keystore.jks
#    password: ${KEYSTORE_PASSWORD:foobar} # don't use a default in production
#    alias: test
    
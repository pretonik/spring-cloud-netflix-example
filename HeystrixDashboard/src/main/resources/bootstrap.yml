spring:
  application:
    name: hystrixdashboard
  cloud:
    config:
      enabled: true
      uri: http://user:s3cret@localhost:8888
#encrypt:
 # failOnError: false
 # keyStore:
 #   location: classpath:keystore.jks
 #   password: ${KEYSTORE_PASSWORD:foobar} # don't use a default in production
 #   alias: test
    